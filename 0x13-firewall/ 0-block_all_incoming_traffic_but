# 1. Install UFW (if not already installed)
sudo apt update
sudo apt install ufw -y

# 2. Deny all incoming traffic by default
sudo ufw default deny incoming

# 3. Allow all outgoing traffic by default
sudo ufw default allow outgoing

# 4. Allow SSH (port 22) for remote access
sudo ufw allow 22/tcp

# 5. Allow HTTP (port 80)
sudo ufw allow 80/tcp

# 6. Allow HTTPS (port 443)
sudo ufw allow 443/tcp

# 7. Enable the firewall (you’ll be warned it may disrupt SSH if not allowed)
sudo ufw enable

# 8. Check the status and rules
sudo ufw status verbose
